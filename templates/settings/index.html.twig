{% extends 'dashboard/layout.html.twig' %}

{% block page_title %}Paramètres{% endblock %}
{% block page_subtitle %}Personnalisez votre expérience{% endblock %}

{% block dashboard_content %}
<div class="row">
    <div class="col-lg-8">
        <!-- Theme Settings -->
        <div class="content-card">
            <div class="card-header">
                <h3 class="card-title">
                    <i class="fas fa-palette"></i>
                    Apparence
                </h3>
            </div>
            <div class="card-body">
                <form method="post" action="{{ path('app_settings_index') }}">
                    <div class="form-group">
                        <label class="form-label">Thème</label>
                        <div class="theme-selector">
                            <div class="theme-option">
                                <input type="radio" name="theme" value="light" id="theme-light" {{ current_theme == 'light' ? 'checked' : '' }}>
                                <label for="theme-light" class="theme-label">
                                    <div class="theme-preview light">
                                        <div class="preview-header"></div>
                                        <div class="preview-content"></div>
                                    </div>
                                    <span>Clair</span>
                                </label>
                            </div>
                            <div class="theme-option">
                                <input type="radio" name="theme" value="dark" id="theme-dark" {{ current_theme == 'dark' ? 'checked' : '' }}>
                                <label for="theme-dark" class="theme-label">
                                    <div class="theme-preview dark">
                                        <div class="preview-header"></div>
                                        <div class="preview-content"></div>
                                    </div>
                                    <span>Sombre</span>
                                </label>
                            </div>
                            <div class="theme-option">
                                <input type="radio" name="theme" value="auto" id="theme-auto" {{ current_theme == 'auto' ? 'checked' : '' }}>
                                <label for="theme-auto" class="theme-label">
                                    <div class="theme-preview auto">
                                        <div class="preview-header"></div>
                                        <div class="preview-content"></div>
                                    </div>
                                    <span>Automatique</span>
                                </label>
                            </div>
                        </div>
                    </div>
                    
                    <div class="form-actions">
                        <button type="submit" class="btn btn-primary-modern btn-modern">
                            <i class="fas fa-save me-2"></i>
                            Sauvegarder
                        </button>
                    </div>
                </form>
            </div>
        </div>

        <!-- Account Settings -->
        <div class="content-card">
            <div class="card-header">
                <h3 class="card-title">
                    <i class="fas fa-user-cog"></i>
                    Compte
                </h3>
            </div>
            <div class="card-body">
                <div class="info-group">
                    <label class="info-label">Nom d'utilisateur</label>
                    <div class="info-value">{{ user.pseudo }}</div>
                </div>
                <div class="info-group">
                    <label class="info-label">Email</label>
                    <div class="info-value">{{ user.email }}</div>
                </div>
                <div class="info-group">
                    <label class="info-label">ID Utilisateur</label>
                    <div class="info-value">#{{ user.id }}</div>
                </div>
                
                <div class="form-actions">
                    <a href="{{ path('app_forgot_password_request') }}" class="btn btn-outline-primary btn-modern">
                        <i class="fas fa-key me-2"></i>
                        Réinitialiser le mot de passe
                    </a>
                </div>
            </div>
        </div>

        <!-- Data Management -->
        <div class="content-card">
            <div class="card-header">
                <h3 class="card-title">
                    <i class="fas fa-database"></i>
                    Gestion des données
                </h3>
            </div>
            <div class="card-body">
                <div class="data-stats">
                    <div class="row">
                        <div class="col-md-4">
                            <div class="stat-item">
                                <div class="stat-number">{{ user.transactions|length }}</div>
                                <div class="stat-label">Transactions</div>
                            </div>
                        </div>
                        <div class="col-md-4">
                            <div class="stat-item">
                                <div class="stat-number">{{ user.isVerified ? 'Oui' : 'Non' }}</div>
                                <div class="stat-label">Email vérifié</div>
                            </div>
                        </div>
                        <div class="col-md-4">
                            <div class="stat-item">
                                <div class="stat-number">{{ user.id }}</div>
                                <div class="stat-label">ID Utilisateur</div>
                            </div>
                        </div>
                    </div>
                </div>
                
                <div class="form-actions">
                    <button class="btn btn-outline-secondary btn-modern" onclick="exportData()">
                        <i class="fas fa-download me-2"></i>
                        Exporter mes données
                    </button>
                </div>
            </div>
        </div>
    </div>
    
    <div class="col-lg-4">
        <!-- Quick Actions -->
        <div class="content-card">
            <div class="card-header">
                <h3 class="card-title">
                    <i class="fas fa-bolt"></i>
                    Actions rapides
                </h3>
            </div>
            <div class="card-body">
                <div class="quick-actions">
                    <a href="{{ path('app_transaction_new') }}" class="quick-action">
                        <i class="fas fa-plus-circle"></i>
                        <span>Nouvelle transaction</span>
                    </a>
                    <a href="{{ path('app_crypto_new') }}" class="quick-action">
                        <i class="fas fa-coins"></i>
                        <span>Ajouter une crypto</span>
                    </a>
                    <a href="{{ path('app_analytics_index') }}" class="quick-action">
                        <i class="fas fa-chart-line"></i>
                        <span>Voir les analyses</span>
                    </a>
                    <a href="{{ path('app_dashboard') }}" class="quick-action">
                        <i class="fas fa-tachometer-alt"></i>
                        <span>Tableau de bord</span>
                    </a>
                </div>
            </div>
        </div>

        <!-- Theme Toggle -->
        <div class="content-card">
            <div class="card-header">
                <h3 class="card-title">
                    <i class="fas fa-toggle-on"></i>
                    Thème rapide
                </h3>
            </div>
            <div class="card-body">
                <div class="theme-toggle-container">
                    <div class="theme-toggle">
                        <input type="checkbox" id="theme-toggle-switch" class="theme-switch">
                        <label for="theme-toggle-switch" class="theme-switch-label">
                            <span class="theme-switch-slider"></span>
                            <span class="theme-switch-text">
                                <span class="light-text">☀️ Clair</span>
                                <span class="dark-text">🌙 Sombre</span>
                            </span>
                        </label>
                    </div>
                    <p class="text-muted small mt-2">Basculez rapidement entre les thèmes clair et sombre</p>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block javascripts %}
{{ parent() }}
<script>
document.addEventListener('DOMContentLoaded', function() {
    // Theme toggle switch
    const themeToggle = document.getElementById('theme-toggle-switch');
    const currentTheme = '{{ current_theme }}';
    
    // Set initial state
    if (currentTheme === 'dark') {
        themeToggle.checked = true;
    }
    
    // Theme toggle functionality
    themeToggle.addEventListener('change', function() {
        const newTheme = this.checked ? 'dark' : 'light';
        
        fetch('{{ path('app_settings_theme', {'theme': 'THEME_PLACEHOLDER'}) }}'.replace('THEME_PLACEHOLDER', newTheme), {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
            }
        })
        .then(response => response.json())
        .then(data => {
            if (data.success) {
                // Apply theme immediately
                applyTheme(newTheme);
                showNotification('Thème changé avec succès!', 'success');
            }
        })
        .catch(error => {
            console.error('Error changing theme:', error);
        });
    });
    
    // Apply theme function
    function applyTheme(theme) {
        document.documentElement.setAttribute('data-theme', theme);
        localStorage.setItem('theme', theme);
    }
    
    // Export data function
    window.exportData = function() {
        // This would typically generate and download a CSV/JSON file
        showNotification('Fonctionnalité d\'export en cours de développement', 'info');
    };
    
    // Initialize theme on page load
    const savedTheme = localStorage.getItem('theme') || currentTheme;
    applyTheme(savedTheme);
});
</script>
{% endblock %}
